/**
 * Carga del catálogo de muestra (IA) en `productos`. Ver docs/CATALOG-GENERATION.md.
 * Placeholder de Bloque 0: el dataset y el upsert se implementan en Bloque 1.
 */
async function seed() {
  console.info("[db:seed] pendiente — se implementa en Bloque 1 (catálogo IA + upsert).");
}

seed();
